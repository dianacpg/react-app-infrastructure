import React, { useState } from "react";
import { hot } from "react-hot-loader";
import './app.scss';

const App = () => {

    return (
        <div className="App">
            <h1>React app!</h1>
        </div>
    );
}
export default hot(module)(App);